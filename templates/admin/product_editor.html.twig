{% extends 'admin/layout.html.twig' %}
{% block title %}{{ title }}{% endblock title %}

{% block admin_content %}
    <h1 class="mb-4">{{ title }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}
