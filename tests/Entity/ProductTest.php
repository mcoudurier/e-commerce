<?php
namespace App\Tests\Entity;

/*
use App\Entity\Product;
use Symfony\Component\Validator\Test\ConstraintValidatorTestCase;

class ProductTest extends ConstraintValidatorTestCase
{
    public function testValidation(ValidatorInterface $validator)
    {
        $product = new Product();
        $product->setName('nom collier');
        $product->setDescription('description collier');
        $product->setCategory('collier');
        $product->setStock(10);
        $product->setPrice(9.99);
        $product->setWeight(0.2);
        $product->setImages([]);

        $errors = $validator->validate($product);
    }
}
*/
